export {};
// This is an example file for the content script. It is not used in the extension.
// All content scripts belong in this folder
// A contentScript typically doesn't export anything, but rather add event listeners to the window/chrome object
